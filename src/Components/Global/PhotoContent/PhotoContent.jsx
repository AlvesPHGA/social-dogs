import React from 'react';
import { PhotoContentStyle } from './PhotoContent.style';

const PhotoContent = ({ data }) => {
   return <PhotoContentStyle></PhotoContentStyle>;
};

export default PhotoContent;
