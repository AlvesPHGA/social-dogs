import React from 'react';
import { Title } from '../../Global/Title';

const NotFound = () => {
   return (
      <section>
         <Title>NotFound</Title>
      </section>
   );
};

export default NotFound;
